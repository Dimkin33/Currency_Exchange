apps:
  - name: myapp
    module: src.main:app
    host: 0.0.0.0
    port: 8000
    workers: 4
    loglevel: info
    reload: true
    env_file: .env  # Указываем, что переменные окружения будут загружаться из .env
